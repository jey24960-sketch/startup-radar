# Live acceptance checkpoint

The current goal is [GFC-OPERATING-GOAL.md](GFC-OPERATING-GOAL.md). Preserve previous implementation and evidence; this checkpoint is not production cutover approval.

## Real data and authorization

The deployed GFC preview at https://gfc-startup-dx0kpwb1l-jey24960-9908s-projects.vercel.app/notice displayed the real K-Startup Pre-WoW opportunity to a dedicated, authenticated **ordinary member**. The account was created with the official Auth admin API and upgraded through GFC's existing one-use invitation RPC, not user-editable role metadata. A separate authenticated external account and a logged-out browser saw the protected-content explanation, without program facts.

Real Auth sessions called context, list and detail RPCs: ordinary member HTTP 200, external HTTP 403/42501, anonymous HTTP 401/42501. An ordinary member's request for an unrelated team returned 403/42501. No mocked API, inserted fixture program, forged token or existing user's role change was used.

Program `ab587732-09e1-44c1-9906-a25af4e813a7`, version `a95a0231-5f48-4177-8fd9-7dffcfdb4264`, preserves the official publisher URL and reviewed attachment coverage. Stage 0 yields NEEDS_INFO and score 72.5 while three participation commitments remain unknown. Original document parsing failures are preserved alongside the reviewed publisher alternatives.

## Real profile writes

The ordinary member created a controlled Stage 0 profile using only a name and preset. Optional personal attributes remained null, with PRE_TEAM/IDEA/PRE_BUSINESS and has_revenue=false as preset defaults. The same user saved an EDUCATION preference through the ordinary profile RPC and a false participation answer through the program-response RPC. Normal batch evaluation produced INELIGIBLE, and the actual browser displayed the denial. The 390px mobile detail had no horizontal overflow. Test answers describe controlled validation scenarios, not a real application or personal commitment.

## Official source operation

The same controlled member then confirmed all three participation conditions. Normal evaluation and the deployed UI showed ELIGIBLE, score 95. Notification planning and delivery now use those current per-program answers and recompute ranking; a queued message is cancelled when its answer snapshot changes. Unknown answers remain unknown and appear as specific questions in digest text.

The manual Telegram scenario check prepares three messages for one named validation team and one official program before any delivery. D-7 and D-3 explicitly simulate the clock without changing source deadlines. It uses the ordinary planner/outbox, limits transport to reviewed texts and the existing configured recipient, rejects repeated delivery, and restores disabled preferences. Local regression checks cover answer changes and the three-scenario lifecycle; real receipts are recorded separately after dispatch.

Both issued official API credentials were accepted in prior real ingestion. The new bounded BizInfo run `bb5821cc-8bb7-4460-849e-1517eee6046c` fetched and persisted five actual opportunities, including two new programs. The shared store reached 37 programs and 62 versions; member refresh computed 35 new results and reused 399. Profile fingerprints, notification item count and scheduling settings were unchanged by ingestion.

This run was PARTIAL_SUCCESS: two scanned PDFs require OCR/review, one AI date lacked verbatim evidence, and the deliberate one-page limit is reported. Earlier live runs proved attachment URL splitting, successful HWP/HWPX extraction, version changes and analysis reuse. Bounded samples do not establish complete nationwide coverage.

## Automation gates

The official API secrets are registered in the Radar repository without exposing their values. Manual workflow_dispatch may run with RADAR_V2_ENABLED=false; scheduled invocations still require RADAR_V2_ENABLED=true. External delivery remains a separate RADAR_V2_DELIVERY_ENABLED gate. Both repository gates and shared DB scheduling remain off during validation. V1's existing disabled-inactivity state is preserved.

Existing accepted evidence includes administrator draft/edit/archive, team switch and preferences flows; a real DIGEST receipt with a second zero-send pass; and baseline Python, GFC, PostgreSQL, Worker and OCR CI. Do not repeat these suites without relevant changes.

Still pending: GitHub-hosted runtime acceptance, high-fit/D-7/D-3 controlled notification evidence, matched-cohort V1/V2 comparison, production GFC deployment and actual recurring operation. Retain test accounts until final production role verification, then revoke sessions and login access.
