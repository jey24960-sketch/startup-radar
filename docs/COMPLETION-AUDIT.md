> Current shared-GFC staging results: see SHARED-GFC-REPORT.md. The original implementation matrix below is retained; current hosted verification and remaining gates are updated separately.

# StartupRadar 2.0 requirement audit

Assessment date: 2026-09-12. Full scope: V2-SPEC.md, sections 0–27. Baseline remote main remains c18f84048ddd08adca10d44b83692a1060d957cb. This assessment covers the local V2 branch, including the DATE/DATETIME precision fix. It is **not a completion or production cutover declaration**.

Evidence levels below are deliberately different: **local** means code plus bounded automated or browser evidence; **partial** means implemented behavior with a remaining validation/coverage limitation; **external** means the real account, service or operating experiment has not been exercised. A fixture transport, mock Auth identity or synthetic notice cannot prove hosted operation.

## Specification sections

| Section | Scope and authoritative implementation evidence | Assessment / remaining proof |
|---|---|---|
| 0 | V1-AUDIT.md identifies real main/core/Worker/Actions execution, duplicate root files, environment/settings/cache/deployment dependencies. core/results.py and V1 tests cover safety fixes; V1 files remain. SafeHttp enforces public HTTPS, host and robots boundaries. core/clock.py supplies Seoul time. | Local audit and safeguards; production baseline was not replaced. Official contracts are documented, no credentials/endpoints invented for missing providers. |
| 1 | models.py separates team/product/business state and supports multiple teams; schema/RLS scopes profiles; recommendations.py excludes policy loans/general finance/generic R&D; layered adapters, PostgreSQL, Telegram and web are separate modules. | Local. Real source breadth and team relevance need an actual operating cohort. No nationwide coverage claim. |
| 2 | PRESETS/apply_preset implement all five requested labels/defaults; explicit values survive preset changes; preset 0 has no optional personal requirements; preset 3 does not infer registration; preset 4 preserves independent product stage. | Local domain tests and preset-0 browser/API flow. Real invited-user flow pending. |
| 3 | TeamProfile has team status, product stage, business state, size, age/band, student/university, region, registration date/age, industry, coarse revenue/investment preferences and global interest. Database.save_profile stores immutable profile versions with optimistic version checks. | Local. Precise home address/birth date are rejected. Real GFC profile has not been populated. |
| 4 | web/src/app.js progressive form and profile PATCH update only supplied fields and re-evaluate. Missing fields remain unknown; registration_date maps to business_registration_date input. | Local API/browser evidence. Hosted Auth is not substituted by the preview override. |
| 5 | SourceAdapter with discover/fetch_detail/fetch_documents/normalize; official.py and longtail.py implement API/RSS/HTML/browser/search strategies. Candidate and source observations retain identity, URLs, timestamps and metadata. | APIs are wired but keys missing locally. Search explicitly disabled without provider. Browser strategy is optional/unvalidated in deployment; no challenge bypass. |
| 6 | documents.py/document_worker.py handle HTML, text PDF, HWP v5, HWPX, DOCX with MIME/hash/status/text/fetch metadata; size/archive/page/text/subprocess limits; failed extraction retained. | Local tests and two real HWP samples. Scanned/OCR, encrypted/unsupported variants and binary archival are documented limits. Windows hard subprocess memory isolation remains limited. |
| 7 | Program and PostgreSQL schema hold canonical identity, title/org/types, dates/deadline/status, original/application URLs, support/benefit/amount fields and version pointers. Dates/status are deterministic. DATE vs DATETIME precision distinguishes calendar data from explicit times. | Local. Suggested fields use requirements/normalized JSON where appropriate. Real normalized catalog accuracy pending. |
| 8 | save_program hashes normalized/raw content, creates versions and material change events for deadlines, requirements, benefits, application URL and documents. | Local update/version tests. Trust resolution across conflicting publishers remains a known limitation. |
| 9 | Publisher identity precedes URL; distinct IDs on shared URLs remain separate; unique corroborated cross-source URL/title/org matches deduplicate; fuzzy/ambiguous candidates remain for review. | Local exact/cross-source/changed-URL/shared-URL/ambiguous tests. Old erroneous merges require reviewed repair; confirmation does not automatically rewrite historical foreign keys. |
| 10 | Requirement schema validates keys/operators/types/evidence; extractor validates quoted evidence, rejects blank/fabricated citations, preserves original structured requirements, marks unsupported OR output uncertain and preserves facts on failure. | Local synthetic evidence tests. Literal quote presence does not prove semantic correctness or complete section extraction; real model/corpus validation pending. |
| 11 | evaluate returns ELIGIBLE/NEEDS_INFO/INELIGIBLE/UNVERIFIABLE with matched/failed/missing/unverifiable/evidence, evaluation date and engine version. Confirmed hard failures take precedence. | Local tests for all required conditions, ages/bands, regions, business age/status, multiple conditions and unknowns. Complex unsupported logic remains review-only. |
| 12 | rank applies eligibility gate, current dates/categories and configured stage/profile/preference/benefit/global/runway/completeness weights. components/provider/model/prompt/schema/explanation are persisted. | Local deterministic ranking and integration evidence. No live AI ranking override. Real ranking usefulness has not been measured. |
| 13 | Five migrations supply 28 RLS-enabled tables covering all required entities, versions, relational constraints, indexes, outbox, jobs, settings, snapshots and audit. V2 uses PostgreSQL, not cache/JSON as primary state. | All migrations/RLS verified locally; hosted Supabase migration/advisor/pooler proof missing. |
| 14 | ingestion and services.health record attempts/success/counts/failures/latency/history; admin failures distinguishes source and parsing errors. Registry disabled reasons and source audit are retained. | Local source failure/partial success tests and public inventory checks. Metric explicitly refers to registered enabled sources, not all Korean programs. |
| 15 | Persistent planners produce grouped weekly digest, exceptional high-fit alerts and D-7/D-3 reminders; eligible vs needs-info separated; version/period dedupe, caps, receipt mapping, revalidation and uncertainty/recovery. | Local real-PostgreSQL/stub-transport tests. Actual Telegram receipt and network-crash exercise missing. |
| 16 | telegram.py checks administrator mapping and update idempotency; /run and /digest dispatch persisted jobs, /status reads actual DB, /stage persists profile versions, /stop updates runtime settings; /team and /health supported. | Local tests; real webhook registration/command round trip pending. V1 Worker safety bridge remains available. |
| 17 | Dashboard covers member A–I: presets/profile/recommendations/browse/filters/detail/evidence/eligibility/history. Admin A–F: source health/run history/failed sources/parsing failures/duplicate review/manual retry. Detail separates FACT, ELIGIBILITY and RECOMMENDATION. | Local browser/API evidence. Hosted login/backend missing. Polished admin invitation/subscription forms are optional remaining UX work; functioning admin APIs/setup exist. |
| 18 | authenticated_user validates token via Supabase get_user; membership checks and RLS isolate team profiles; no user_metadata authorization, no browser service key; public config exposes publishable setup only. | Anonymous/cross-team/admin escalation tests pass locally. Actual Auth session/invitation and deployed boundaries not proven. |
| 19 | V2 workflow wakes hourly; database controls daily ingestion/weekly digest/reminder cadence, separate delivery flag, concurrency group and advisory lock; incomplete work exits nonzero. | Local cadence/concurrency tests and workflow review. Actual GitHub execution and secrets setup missing. |
| 20 | Typed source/API/document/AI failures are distinct from empty results; partial ingestion preserves valid data; V1 reliability tests preserve valid partial output and nonzero failures. | Local. No actual AI call made in V2 validation. |
| 21 | Admin trace links recommendation/evaluation/profile/program version, requirements, documents, source snapshots/config/extractor metadata and notification state. | Local trace/snapshot tests. Binary source archival and publisher conflict resolution remain limits. |
| 22 | V1 preserved through staged local milestones; parallel.py/CLI compare records V1-only/V2-only, ambiguous duplicates, eligibility review, failures and collection-window limits without authorizing cutover. | Milestone 10 incomplete: no live matched-period comparison or V1 retirement. |
| 23 | Required automated behavior groups mapped below. Full latest native suite: 139 passed. | Local test evidence, not proof of remote services. |
| 24 | Twenty acceptance items are separately mapped in V2-PROGRESS.md. Local behavior supports the items; hosted and actual-delivery proof remain explicit gaps. | Full acceptance unproven. |
| 25 | README, V1-AUDIT, ARCHITECTURE, SOURCES/Adding a Source, V2-SETUP, OPERATIONS and V2-PROGRESS document architecture, environment, migrations, eligibility/ranking, deployment, retries, security and migration. Output code ZIP and detailed Korean introduction are supplied. | Documents supplied. Final operating handoff/cutover cannot be claimed before external gates. |
| 26 | No nationwide promise, broad bypass, fabricated eligibility, mandatory full-profile onboarding, linear-only state, V2 cache persistence, Git profile updates or broadcast-to-every-team behavior. | Code and scope review; source/AI failures remain explicit. |
| 27 | Evidence, eligibility, team relevance and deadline correctness precede visual work; all channels link original notices and expose uncertainty. | Product principle implemented locally; actual opportunity utility needs user/cohort validation. |

## Required test evidence (section 23)

| Required cases | Test sources |
|---|---|
| Stage 0; unknown fields; explicit values preserved; independent MVP/registration | test_eligibility.py preset and unknown tests; test_web_runtime.py stage-zero/progressive flows |
| All satisfied; one hard failure; missing data; unavailable evidence; multiple conditions | test_eligibility.py; test_extraction_evidence.py unsupported OR and preserved structured rule tests |
| Age ranges/bands; regions; business age; pre-business/registered-only | test_eligibility.py and cross-team program API assertion in test_web_runtime.py |
| Open/upcoming/closed; D-7/D-3; rolling; Seoul boundary and exact cutoff | test_eligibility.py dates; test_database.py reminder delivery; test_web_runtime.py expiration; test_extraction_evidence.py time cases |
| Multi-source identity; URL changes; deadline extension; title normalization; fuzzy ambiguity | test_database.py identity/version suite and test_parallel.py ambiguity tests |
| Actually delivered items only; partial failure; duplicate prevention; reminder idempotency; material update once | test_v1_reliability.py, test_database.py, test_web_runtime.py, test_native_concurrency.py |
| Source isolation; empty API vs failure; document parse; malformed AI/schema | test_ingestion_units.py, test_document_process.py, test_database.py, test_extraction_evidence.py |
| Unauthorized/duplicate Telegram; RLS/team isolation | tests/worker.test.mjs, tests/database.test.mjs, test_web_runtime.py and test_database.py |

The earlier 139-test checkpoint used native PostgreSQL 18.4 and finished successfully with one unchanged third-party deprecation warning. Fresh migration/RLS and four Worker checks passed earlier; no migration/Worker change followed those checks. The current frontend was rebuilt and browser-checked: unknown/coarse times display “시각 원문 확인”, while an explicit fixture DATETIME displays “오전 11:00”. Preview uses fictional notices and was stopped after inspection.

## External completion gates and next actions

1. **Choose the web staging host after the A/B/C audit.** Supabase is now selected: the existing GFC project etvffzxqdgblvkfdikwl, with isolated startup_radar schema. WEB-INTEGRATION.md recommends gradual integration (C). No new Supabase project is needed.
2. **Configure remaining V2 runtime credentials securely.** The project URL and modern publishable key are prepared in ignored .env.staging. DATABASE_URL, AI/official API keys, Telegram credentials and GitHub dispatch credentials are still needed for the Python runtime. This says nothing about inaccessible existing GitHub/production secrets.
3. **Complete real Auth/runtime connectivity.** Two hosted migrations, advisor checks and SQL role/claim team-isolation tests now pass. GFC object definitions and data remained unchanged. Preserve existing GFC Auth providers/signup/site settings. Designate Radar memberships and exercise real browser JWT login through the chosen backend/pooler.
4. **Run real official ingestion and a labeled notice cohort.** Verify pagination/contracts, documents and extraction quality, then investigate eligibility/ranking against original evidence. Synthetic test counts do not substitute for this.
5. **Exercise an intended test delivery/workflow.** Use a designated test bot/chat and verify receipts, commands, failure/recovery and the chosen pooler. No external messages were sent in this session.
6. **Run matched-period V1/V2 comparisons and assess reliability.** Only after this proof should the production webhook/schedule switch be performed. V1 remains present and production unchanged.

These are actual setup and operating gates, not missing code that can be replaced by more fixture tests. Optional future hardening (OCR, broader source-specific pagination, polished invitation forms, binary retention, automated paging and catalog query scaling) is documented separately and does not establish hosted acceptance.
